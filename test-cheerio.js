const cheerio = require('cheerio'); fetch('https://propertyhub.in.th/en/listings/1-bedroom-condo-in-the-riviera-jomtien-21808620', { headers: { 'User-Agent': 'Mozilla/5.0' } }).then(async r => { const html = await r.text(); const $ = cheerio.load(html); const imgs = []; $('img').each((i, el) => { let src = $(el).attr('src') || $(el).attr('data-src'); if (src) imgs.push(src); }); console.log('Total img elements:', imgs.length); console.log(imgs.slice(0, 10)); }).catch(console.error)
